{
  "name": "csw-claude-plugin",
  "version": "0.1.0",
  "description": "Claude Code plugin for CSW Agent — Instagram post management and WeChat article generation via MCP",
  "type": "module",
  "bin": {
    "csw-claude-plugin": "./dist/index.js"
  },
  "files": [
    "dist",
    "skills",
    "commands",
    ".claude-plugin",
    "install.sh"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.28.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "keywords": ["claude-code", "mcp", "csw", "wechat", "instagram"]
}
