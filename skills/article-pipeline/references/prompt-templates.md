# Prompt Templates

Prompt templates are fetched at runtime from the CSW backend:

Use the MCP tool `csw_prompts_get` to retrieve per-section-type prompt templates configured by the user in the CSW console.

The agent should use these templates as the base for LLM content generation.
